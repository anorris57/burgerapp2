<h1>Burgers!</h1>

<h2>Burgers that are ready to order and not yet devoured! <br> Hit button to order and devour!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been ordered and devoured!
  <br> Add to que if you would like to order.</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger to devour now or order later</h2>
<form class="create-form">

  <div class="form-group">
    <label for="br">Burger Name:</label>
    <input type="text" id="br" name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">What would you like to do with your new burger?</label><br>
    <input type="radio" name="devoured" value="1" checked>  Eat now: Devoured!<br>
    <input type="radio" name="devoured" value="0"> Put in que.
  </div>

  <button type="submit">Add Burger</button>
</form>
